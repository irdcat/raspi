spring:
  webflux:
    base-path: /v2/fitness
  data:
    mongodb:
      database: fitness
      host: localhost
      port: ${mongodb.container.port}

logging:
  level:
    irdcat.fitness.service: DEBUG