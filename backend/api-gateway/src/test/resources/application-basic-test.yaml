application:
  services:
    - name: cookbook-server
      prefix: /cookbook/api
      redirectTo: http://localhost:8083
    - name: cookbook-ui
      prefix: /cookbook
      redirectTo: http://localhost:3000
    - name: food-service
      prefix: /food
      redirectTo: http://localhost:8084