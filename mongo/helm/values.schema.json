{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": [
        "domain",
        "image",
        "development"
    ],
    "properties": {
        "domain": {
            "type": "string",
            "enum": [
                "raspi.local"
            ]
        },
        "image": {
            "type": "object",
            "required": [
                "repository",
                "tag"
            ],
            "properties": {
                "repository": {
                    "type": "string",
                    "pattern": "^[a-z0-9_/\\-]+$"
                },
                "tag": {
                    "type": "string",
                    "pattern": "^[a-zA-Z0-9_\\-]+$"
                }
            }
        },
        "development": {
            "type": "boolean"
        }
    }
}